export type { Product } from '@/lib/types'

export interface HeroSlide {
  id?: string
  title: string
  description: string
  buttonText: string
  buttonLink: string
  image?: string
  icon?: string
}
